import { UI } from '../modules/UI.js';
import { ToDoList } from '../modules/ToDoList.js';
import {handleKeyPress, handleDeleteTaskClick, HandleClearAll } from '../index.js';

describe('handleKeyPress', () => {
    it('should create a new ToDoList and display it when Enter key is pressed', () => {
           // Arrange
    })
  
});