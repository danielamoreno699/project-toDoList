// eslint-disable-next-line no-unused-vars
import ToDoList from '../modules/ToDoList.js';

describe('ToDoList', () => {
  describe('reorderList', () => {
    it('it moves the order of index of the items inside the array', () => {
      // const todo1 = new ToDoList('hola', false, 1);
      // const todo = new ToDoList();
      // todo.reorderTodoList([1, 2, 3, 4]);
    });
  });
});